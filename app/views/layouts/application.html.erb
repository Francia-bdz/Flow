<!DOCTYPE html>
<html>
  <head>
    <title>Flow</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <script src="https://cdn.jsdelivr.net/npm/rails-ujs@5.2.7/lib/assets/compiled/rails-ujs.min.js"></script>
  

  </head>

  <body>
    
    <div class="container-fluid ">
    <div class="square-shadow">
      <%= yield %>

<br/>
      <% if user_signed_in? %>
        <p> Votre compte : <%= link_to current_user.email, edit_user_registration_path, class: "a" %> </p>
        <br/>
        <button class="btn btn-pink bg-gradient text-white">
        <%= link_to "Se déconnecter", destroy_user_session_path, class: "a a-white a-logout", method: :delete %>
        </button>
      <% else %>
   
      <% end %>
      
      </div>
      </div>
      <footer class="footer fixed-bottom bg-black"> <p class="title-site"> FLOW </p> </footer>

  </body>
 
</html>
